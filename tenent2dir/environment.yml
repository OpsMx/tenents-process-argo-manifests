apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp
spec:
  selector:
    matchLabels:
      app: frontend-deployment
  spec:
    containers:
    - name: oooo
      ports:
      - containerPort: "9911"
        name: http
  template:
    metadata:
      labels:
        app: frontend-deployment
    spec:
      containers:
      - name: pear
        ports:
        - containerPort: "3442"
      - image: foo/bar:latest
        name: app
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
